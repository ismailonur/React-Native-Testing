const localizaton = require("./localization");

let locale = new localizaton();

locale.set(`en`)({ error: `General Error!` });
locale.set(`tr`)({ error: `Genel Hata {0}` });


console.log(locale.translate(`tr`)(`error`, `Test`));